---
- name: Install nginx
  apt:
    name: nginx
    state: present

- name: Deploy nginx config
  template:
    src: nginx.conf.j2
    dest: /etc/nginx/conf.d/app.conf
    mode: '0644'
  notify: restart nginx

- name: Start nginx
  service:
    name: nginx
    state: started
    enabled: yes
